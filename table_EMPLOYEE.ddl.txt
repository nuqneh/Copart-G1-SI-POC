   CREATE TABLE EMPLOYEE
     (EMPNO      INTEGER GENERATED ALWAYS AS IDENTITY,
      ID         SMALLINT,
      LAST NAME  CHAR(30),
      FIRST NAME CHAR(30),
      SALARY     DECIMAL(5,2),
      DEPTNO     SMALLINT);

   CREATE UNIQUE INDEX DEPTNO_NAME
     ON EMPLOYEE (DEPTNO, NAME);

